<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mikke_000.greetings">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Game" />
        <activity android:name=".gameAI" />
        <activity android:name=".GameAIFirst" />
        <activity android:name=".chooseAI" />
        <activity android:name=".backup" />
        <activity android:name=".boardSizeSelect" />
        <activity android:name=".gameMP" />
        <activity android:name=".cell" />
        <activity android:name=".Cell" />
        <activity android:name=".Board"></activity>
    </application>

</manifest>